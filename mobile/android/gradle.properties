org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# System only has openjdk-25-jre (no javac) installed system-wide; the project
# targets Java 17 (see app/build.gradle.kts). Use the Temurin JDK already
# installed alongside the Flutter SDK — it has jlink/jmod, unlike the
# PyCharm-bundled JBR, which Android's core-for-system-modules transform needs.
org.gradle.java.home=/home/ekko-7/development/jdk-21.0.11+10
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
org.gradle.java.home=/opt/pycharm/jbr
